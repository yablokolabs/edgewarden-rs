[package]
name = "edge-telemetry"
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Prometheus telemetry and metrics endpoint for edge appliances"
repository = "https://github.com/yablokolabs/edgewarden-rs"

[dependencies]
prometheus = { workspace = true }
axum = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
