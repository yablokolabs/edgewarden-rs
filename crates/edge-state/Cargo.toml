[package]
name = "edge-state"
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Desired vs reported state reconciliation with durable last-known-good for edge fleets"
repository = "https://github.com/yablokolabs/edgewarden-rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "reconcile"
harness = false
