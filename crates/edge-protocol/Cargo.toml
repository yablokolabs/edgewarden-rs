[package]
name = "edge-protocol"
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Outbound-only fleet gRPC protocol and reconnect backoff for cloud-managed edge appliances"
repository = "https://github.com/yablokolabs/edgewarden-rs"

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }
tokio = { workspace = true }
tokio-stream = { workspace = true }
rand = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }
